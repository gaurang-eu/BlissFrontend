import { LoaderColorDirective } from './loader-color.directive';

describe('LoaderColorDirective', () => {
  it('should create an instance', () => {
    const directive = new LoaderColorDirective();
    expect(directive).toBeTruthy();
  });
});
